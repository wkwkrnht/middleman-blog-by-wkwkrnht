ul
  - blog.articles.group_by {|a| a.date.year }.each do |year, articles|
    li
      = link_to article.year, year
      ul
        - articles.each do |article|
          li
            = link_to article.url, article.title